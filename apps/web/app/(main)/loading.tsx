import { PageLoading } from "@/components/ui/page-loading";

export default function MainLoading() {
  return (
    <PageLoading className="min-h-0 flex-1 justify-center" message="Loading…" />
  );
}
