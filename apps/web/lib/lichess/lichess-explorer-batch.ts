/**
 * Maximum FENs accepted per `api.lichess_explorer.batchExplorerMasters` call.
 * Used by the Convex action and client batching — keep definitions in sync.
 */
export const LICHESS_EXPLORER_MAX_FENS_PER_BATCH = 32;
